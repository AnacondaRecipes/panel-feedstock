{% set version = "0.14.2" %}

package:
  name: panel
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/p/panel/panel-{{ version }}.tar.gz
  sha256: c22690833dfa508d72ae632e2d42ea7d63ee2a31347e23b7ecf8dcded312ec74

build:
  number: 0
  # nodejs currently isn't available on s390x
  skip: True  # [py<37 or s390x]
  entry_points:
    - panel = panel.command:main
  script:
    - {{ PYTHON }} -m pip install . -vv

requirements:
  host:
    - python
    - pip
    - setuptools >=42
    - wheel
    # These are also needed at build time.
    - bleach
    - bokeh >=2.4.3,<2.5.0
    - nodejs 
    - pyct
    - packaging
    - param >=1.9.2
    - pyviz_comms >=0.7.4
    - requests
    - tqdm
  run:
    - python
    - bleach
    - bokeh >=2.4.0,<2.5.0
    - markdown
    - param >=1.12.0
    - pyct >=0.4.4
    - pyviz_comms >=0.7.4
    - requests
    # Add setuptools to the runtime dependencies,
    # see https://github.com/holoviz/panel/commit/ae8e460a0ba8bfc4c3c670df156df66ca3dd4d2a
    - setuptools >=42
    - tqdm >=4.48.0
    - typing_extensions

test:
  imports:
    - panel
    - panel.io
  requires:
    - pip
  commands:
    - pip check
    - panel --help

about:
  home: https://panel.holoviz.org
  summary: A high-level app and dashboarding solution for Python
  description: |
    Panel provides tools for easily composing widgets, plots, tables, and other
    viewable objects and controls into control panels, apps, and dashboards.
  license: BSD-3-Clause
  license_family: BSD
  license_file: LICENSE.txt
  doc_url: https://panel.holoviz.org/
  dev_url: https://github.com/holoviz/panel

extra:
  recipe-maintainers:
    - philippjfr
    - ocefpaf
  skip-lints:
    - uses_setuptools
